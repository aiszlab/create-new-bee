import { useNavigate } from 'react-router-dom'

const Home = () => {
  const navigate = useNavigate()

  const onNavigate = () => {
    navigate('/about')
  }

  return (
    <>
      Home
      <button onClick={onNavigate}>前往about</button>
    </>
  )
}

export default Home
